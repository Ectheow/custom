(let* ([pipes (pipe)]
       [pid (primitive-fork)])
  (cond
   [pid ; parent - producer
    (close (car pipes))
    (display "hellocats\n" (cdr pipes))]
   [else
    (close (cdr pipes))
    (format #t "output: '~a'~%" (read-line (car pipes)))]))
    
    
       
  

